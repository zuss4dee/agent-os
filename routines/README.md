# Routines

A routine is a triggered sequence (schedule, webhook, or human trigger) that may invoke agents, skills, and tools. It is not a skill and not an identity.

This directory is a placeholder. Do not encode fake schedulers here.

When routines exist they should be versioned, listed in `registry` (optional `routines` key), and evaluated like everything else.
